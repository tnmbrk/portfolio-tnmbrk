{
  "name": "portfolio-toni",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "npm run prettier && parcel",
    "re:build": "npm run prettier && npm run build:css && parcel build",
    "build": "npm run build:css && parcel build",
    "build:css": "tailwindcss -i ./src/css/source.css -o ./src/style.css",
    "prettier": "prettier --write ./src/"
  },
  "devDependencies": {
    "@parcel/compressor-brotli": "^2.16.0",
    "@parcel/compressor-gzip": "^2.16.0",
    "@parcel/packager-raw-url": "^2.16.0",
    "@parcel/transformer-webmanifest": "^2.16.0",
    "@tailwindcss/forms": "^0.5.10",
    "parcel": "latest",
    "posthtml-doctype": "^1.1.1",
    "posthtml-include": "^2.0.1",
    "prettier": "3.6.2",
    "sharp": "^0.33.5",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "@parcel/optimizer-htmlnano": "^2.16.0",
    "@tailwindcss/cli": "^4.1.13",
    "preline": "^3.2.3",
    "tailwindcss": "^4.1.13"
  },
  "nodemonConfig": {
    "ext": "js,html",
    "watch": [
      "src"
    ]
  }
}
